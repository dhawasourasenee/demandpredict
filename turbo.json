{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["ANTHROPIC_API_KEY", "CLAUDE_MODEL"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {}
  }
}
